#include <stdio.h>
/**
 * rev_string - Entry point
 * @s: character
 * Return: Always 0 (Success)
 */
void rev_string(char *s)
{
	int i;
	int g;


	for (i = 0; s[i] != '\0'; i++)
	{
	}

	char *yeah[i -1];

	for (g = i - 1; g >= 0; g--)
	{
		yeah = s[g];
	}
	*s = yeah;
}
