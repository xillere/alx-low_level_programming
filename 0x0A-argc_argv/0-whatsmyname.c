#include <stdio.h>
/**
 * main - Entry point
 * @argc: count
 * @argv: strings
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s/n", *argv);

	return (0);
}
