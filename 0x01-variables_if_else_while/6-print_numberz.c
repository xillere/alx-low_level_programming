#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	num = '0';

	while (num < 58)
	{
		putchar(num);
		num++;
	}
	putcar('\n');
	return (0);
}
